function petShop(input){
    let dogFood = 2.5
    let catFood = 4
    Number(buyedDogFood = dogFood * input[0])
    Number(buyedCatFood = catFood * input[1])
    console.log(buyedCatFood + buyedDogFood +" lv.")
}
petShop([13,9])